# Backend (groundstack / Railway) environment — NEVER commit real values.

# Supabase — service role key bypasses RLS; backend only.
SUPABASE_URL=https://afygsdlzrfgsvbhkvtxc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Fernet AES-256 key for credential encryption.
# Generate ONCE: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=your-generated-fernet-key

# Stripe
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Protects POST /onboard/create-link (only Vishal should call it).
ADMIN_API_KEY=set-a-long-random-string

# Where the onboarding links point (Vite frontend).
FRONTEND_URL=http://localhost:8080

# Optional: email provider for the welcome email (see emails.py).
EMAIL_API_KEY=
